// examples/demo.ltl — runnable demo for ltl_grep_color

import std::io
use ltl_grep_color as lib

fn main() {
    io::println("ltl_grep_color demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}